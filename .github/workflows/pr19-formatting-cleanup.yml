name: PR19 formatting cleanup
on:
  push:
    branches: [design/typed-inline-content-payloads]
permissions:
  contents: write
jobs:
  rewrite:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - run: python .github/pr19_formatting_cleanup.py
      - run: |
          git rm .github/pr19_formatting_cleanup.py .github/workflows/pr19-formatting-cleanup.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add README.md SCAFFOLDING_PLAN.md docs
          git commit -m "Separate engine text collaboration from application formatting"
          git push origin HEAD:${GITHUB_REF_NAME}
