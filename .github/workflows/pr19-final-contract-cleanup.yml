name: PR19 final contract cleanup
on:
  push:
    branches: [design/typed-inline-content-payloads]
permissions:
  contents: write
jobs:
  rewrite:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - run: python .github/pr19_final_contract_cleanup.py
      - run: |
          git rm .github/pr19_final_contract_cleanup.py .github/workflows/pr19-final-contract-cleanup.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add README.md SCAFFOLDING_PLAN.md docs
          git commit -m "Finalize application-owned formatting boundary"
          git push origin HEAD:${GITHUB_REF_NAME}
