name: PR19 final audit fix
on:
  push:
    branches: [design/typed-inline-content-payloads]
permissions:
  contents: write
jobs:
  rewrite:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - run: python .github/pr19_audit_fix.py
      - run: |
          git rm .github/pr19_audit_fix.py .github/workflows/pr19-audit-fix.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add SCAFFOLDING_PLAN.md docs
          git commit -m "Tighten final payload contract references"
          git push origin HEAD:${GITHUB_REF_NAME}
